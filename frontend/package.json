{
  "name": "bathy-labeler-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc && vite build",
    "test:frontend": "tsc -p tsconfig.test.json && node --test .test-dist/tests/frontend/*.test.js",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "@deck.gl/core": "^9.0.0",
    "@deck.gl/layers": "^9.0.0",
    "@deck.gl/mapbox": "^9.0.0",
    "maplibre-gl": "^5.0.0",
    "plotly.js-dist-min": "^2.35.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
